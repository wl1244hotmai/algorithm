#include"全排列.H"
#include"全组合.H"
int main(int argv, char* argc[]) {
	//全排列 交换法:
	//Permutation permutation;
	//permutation.displayPermutation(0);

	//全排列 字典序
	//Permutation permutation;
	//permutation.displayPermutation_2(0);

	Subsets subsets;
	subsets.allSets();

	return 0;
}